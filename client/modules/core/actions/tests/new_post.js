const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../new_post';

describe('core.actions.new_post', () => {
  it('should do something');
});
